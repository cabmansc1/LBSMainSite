# Missing Image Alt Text — Phase 2 audit

Generated by `_seo/_audit_alt.py`. Two sections:

1. **Missing `alt` attribute entirely** — these will fail accessibility + SEO checks. Andrew needs to write alt text describing what's in each image.
2. **`alt=""` (empty)** — valid for purely decorative images (icons, spacers, background flourishes). Andrew should confirm each really is decorative; if it conveys meaning, write descriptive alt text.

Dynamic `src` values (PHP echos) still need alt text — write something descriptive based on what the image *role* is, e.g. `alt="<?= htmlspecialchars($business['name']) ?> photo"`.

**Note:** the Meta Pixel `noscript` image (`facebook.com/tr?id=…`) is suppressed from this report. It's auto-injected by `seo_head.php` on every page; one `alt=""` added there fixes all instances at once. Track that as its own one-liner task.

---

## 1. Missing `alt` attribute (1 images across 1 files)

### `admin/manage_directory.php`

- [ ] **line 1583** — `src="' + e.target.result + '"`
  - `<img src="' + e.target.result + '" style="width:100%; height:100px; object-fit:cover; display:block;">`

---

## 2. `alt=""` empty (confirm decorative) — 3 images across 3 files

### `business.php`

- [ ] **line 845** — `src=""` — confirm decorative; if not, add alt text

### `admin/manage_blog.php`

- [ ] **line 136** — `src="/uploads/blog/<?php echo htmlspecialchars($post['featured_image']); ?>"` — confirm decorative; if not, add alt text

### `admin/manage_directory.php`

- [ ] **line 1371** — `src="../uploads/business_photos/<?= htmlspecialchars($logo) ?>"` — confirm decorative; if not, add alt text

